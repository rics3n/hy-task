default: &default
  host: <%= ENV['OCR_PORT_5000_TCP_ADDR'] %>
  port: <%= ENV['OCR_PORT_5000_TCP_PORT'] %>

development:
  <<: *default

test:
  <<: *default

staging:
  <<: *default

production:
  <<: *default